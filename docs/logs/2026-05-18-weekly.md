# 2026-05-18 - Weekly digest

Covers the week of 2026-05-12 → 2026-05-18. Replaces the per-day normalization logs (now archived).

## What actually changed

- **Full CSV ingest restored** with content categorisation: all 379 194 rows land in SQLite with an `ingest_category` and `cleaning_applied` tag list. Scripts: `ui/scripts/ingest-csv.ts`, `ui/scripts/analyze-csv.ts`. See [data decisions](../decisions/data.md#2026-05-16---categorise-all-csv-rows-instead-of-dropping).
- **Audio workaround landed** for `data.opencouncil.gr` CORS + malformed-MP3 decoding: Vercel proxy at `/api/audio` plus a fixed-file URL map applied to `corrections.audio_cdn_url`. See [audio decisions](../decisions/audio.md#2026-05-16---audio-workaround-via-vercel-proxy-and-fixed-file-map-pending-proper-fix). Preferred long-term fix (source-side CORS + MP3 repair) remains outstanding.
- **UI polish**: waveform switched to bar rendering, peaks-cache prefetch for next item, eager preload of next page's server data. See [UI decisions](../decisions/ui.md#2026-05-15---waveform-bars--peaks-cache-prefetch).
- **Hosted state**: Turso + Vercel chosen for the deployed prototype; migration script in `ui/scripts/migrate-to-turso.ts`. See [storage decisions](../decisions/storage.md#2026-05-15---turso--vercel-for-hosted-review-state).
- **Docs reorganisation**: `docs/decisions.md` split by theme into `docs/decisions/{data,storage,ui,audio,matching}.md`; daily-normalization logs archived; weekly cadence adopted; `docs/progress.md` added.

## Still open

- Cached meeting JSON per meeting and correction-to-utterance matching are still not implemented — the largest gap toward closing Phase 1.
- Whether to replace the GitHub Release audio mirror with a durable bucket (R2 / Vercel Blob).
- Whether source OpenCouncil audio can be repaired upstream, removing the need for `audio_cdn_url` mappings.
- Whether `data/decode-failures.jsonl` should remain tracked now that generated repair artifacts are gitignored.
