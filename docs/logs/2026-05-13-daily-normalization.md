# 2026-05-13 - Daily Normalization

## Summary

Reviewed files changed in the last 24 hours and normalized project state after the first SvelteKit correction-review prototype appeared under `ui/`.

## Changes

- Updated `CURRENT.md` to reflect the implemented local UI baseline, SQLite ingest/state, JSONL event log path, and the remaining need for meeting JSON matching.
- Updated `docs/roadmap.md` PRD todos for completed local storage, ingest, review UI, stats, label persistence, and export work.
- Updated `docs/specs/local-data-model.md` to distinguish implemented SQLite review tables from planned meeting-cache and matched-correction tables.
- Replaced the default Svelte scaffold text in `ui/README.md` with prototype-specific setup, state, and capability notes.
- Linked the UI prototype from `docs/project-map.md` and adjusted the immediate workflow.
- Corrected `docs/reference/opencouncil-meeting-json.md` so matching compares CSV `after_text` to JSON `utterance.text`.

## Left Unchanged

- Meeting notes and meeting index already point to canonical summaries and did not need restructuring.
- Archived raw/superseded notes already live under `archive/`.
- Data-quality reports still describe the historical sample preprocessing outputs and were not rewritten.

## Ambiguous / Human Review

- `data/reports/data_quality.md` is based on `archive/old-data/corrections-sample.csv`, while the current UI ingests the full May 12 export. Decide whether to run a new full-export quality report or keep the sample report as historical only.
- `ui/data/corrections.sqlite` is large local state. Decide whether it should remain local-only, be regenerated by `ui/scripts/ingest.ts`, or be treated as a tracked artifact outside this vault workflow.
